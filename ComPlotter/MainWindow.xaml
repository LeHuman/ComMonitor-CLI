<mah:MetroWindow
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ComPlotter"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        xmlns:collections="clr-namespace:System.Collections;assembly=System.Runtime"
        xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Modern"
        x:Class="ComPlotter.MainWindow"
        GlowBrush="{DynamicResource AccentColorBrush}"
        d:DataContext="{d:DesignData Type=local:MainWindowVM, IsDesignTimeCreatable=False}"
        Title="ComPlotter" Height="480" Width="800" TitleCharacterCasing="Normal">

    <mah:MetroWindow.LeftWindowCommands>
        <mah:WindowCommands>
            <Button Click="LaunchGitHubSite"  ToolTip="Open up the GitHub site">
                <iconPacks:PackIconModern Width="22"
                                  Height="22"
                                  Kind="SocialGithubOctocat" />
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.LeftWindowCommands>

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <Button Click="DeployCupCakes" Content="Deploy CupCakes">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Width="22"
                                        Height="22"
                                        VerticalAlignment="Center"
                                        Kind="FoodCupcake" />
                            <TextBlock Margin="4 0 0 0"
                         VerticalAlignment="Center"
                         Text="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <Grid>
        <TabControl>
            <TabItem Header="Graph" FontSize="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="200" />
                    </Grid.ColumnDefinitions>

                    <local:GraphControl Margin="20" Controller="{Binding MultiController}" />

                    <Border Grid.Column="1" Margin="10">
                        <StackPanel>
                            <TextBlock >Duration</TextBlock>
                            <TextBox Text="{Binding MultiController.Range.MaximumX,Mode=TwoWay}"></TextBox>

                            <TextBlock Margin="0 10 0 0">Refresh Rate</TextBlock>
                            <TextBox Text="{Binding MultiController.RefreshRate,Mode=TwoWay}"></TextBox>

                            <TextBlock Margin="0 10 0 0">Minimum Y</TextBlock>
                            <TextBox Text="{Binding MultiController.Range.MinimumY,Mode=TwoWay}"></TextBox>

                            <TextBlock Margin="0 10 0 0">Maximum Y</TextBlock>
                            <TextBox Text="{Binding MultiController.Range.MaximumY,Mode=TwoWay}"></TextBox>

                            <CheckBox Margin="0 5 0 0" IsChecked="{Binding MultiController.Range.AutoY,Mode=TwoWay}">Auto Range (Y)</CheckBox>

                            <ListBox ItemsSource="{Binding MultiController.DataSeriesCollection}" Height ="125" MaxHeight="125" MinHeight="50" Margin="0 5">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel>
                                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsVisible}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Button Margin="0 10 0 0" Padding="5" Command="{Binding MultiController.ClearCommand}">Clear</Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            <TabItem Header="Settings">
                <Grid>
                </Grid>
            </TabItem>
            <TabItem Header="About">
                <Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</mah:MetroWindow>
