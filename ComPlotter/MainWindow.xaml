<mah:MetroWindow
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ComPlotter"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Modern"
        x:Class="ComPlotter.MainWindow"
        GlowBrush="{DynamicResource AccentColorBrush}"
        d:DataContext="{d:DesignData Type=local:MainWindow, IsDesignTimeCreatable=False}"
        Title="ComPlotter" Height="480" Width="1128" TitleCharacterCasing="Normal">

    <mah:MetroWindow.LeftWindowCommands>
        <mah:WindowCommands>
            <Button Click="LaunchGitHubSite"  ToolTip="Open up the GitHub site">
                <iconPacks:PackIconModern Width="20"
                                  Height="20"
                                          VerticalAlignment="Center"
                                  Kind="InformationCircle" />
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.LeftWindowCommands>

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <Button Click="DeployCupCakes">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Width="20"
                                        Height="20"
                                        VerticalAlignment="Center"
                                        Kind="Settings" />
                            <TextBlock Margin="4 0 0 0"
                         VerticalAlignment="Center"
                         Text="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45*" />
            <ColumnDefinition Width="10*" />
        </Grid.ColumnDefinitions>

        <WpfPlot Name="MainPlot" />

        <Border Grid.Column="1" Margin="10">
            <StackPanel>
                <TextBlock >Sample Range</TextBlock>
                <TextBox Text="{Binding PlotController.Range,Mode=TwoWay}"></TextBox>

                <CheckBox Margin="0 5 0 0" IsChecked="{Binding PlotController.AutoRange,Mode=TwoWay}">Auto Fit</CheckBox>

                <ListBox ItemsSource="{Binding PlotController.SeriesManager.Series}" MaxHeight="200" Margin="0 5">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <CheckBox Content="{Binding Name}" IsChecked="{Binding SignalPlot.IsVisible}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button Margin="0 5 0 0" Padding="5" VerticalAlignment="Bottom" >Apply</Button>
                <Button Margin="0 5 0 0" Padding="5" Command="{Binding PlotController.ClearCommand}" VerticalAlignment="Bottom">Clear</Button>
            </StackPanel>
        </Border>
    </Grid>
</mah:MetroWindow>
